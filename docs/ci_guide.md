
### CI Guide
# Overview
    --> Purpose of CI in this project.
    --> Tools used (GitHub Actions / GitLab CI / CircleCI / Jenkins, etc.)

# Pipeline Structure
    --> Stages (build → test → lint → deploy).
    --> Diagram of the pipeline workflow.

# Files & Locations
    --> Where CI config files live (e.g., .github/workflows/*).

# How CI is Triggered
    --> Trigger types (push, PR, schedule).
    --> Branch rules.

# Jobs & Steps
    # Build Job
        --> What is built and how.

    # Test Job
        --> Tests executed, test frameworks, coverage.

    # Lint Job
        --> Linting tools and conventions.

    # Deploy Job
        --> Deployment environment & steps.

    # Adding New CI Jobs
        --> How to create or modify CI workflows.

    # Troubleshooting CI Failures
        --> Reading logs.
        --> Common errors and solutions.

    # Best Practices
        --> Caching dependencies.
        --> Keeping pipelines fast.

